# ================================================================= 壳配置 =================================================================

# 服务名称，用于标识当前部署的应用服务
SERVICE_NAME=dub_precedent

# 环境变量文件名，容器启动时会加载该文件
ENV_FILE=.env

# 应用构建上下文目录，用于 Docker 构建时定位项目源码
APP_CONTEXT=dub_precedent

# Docker 镜像名称和标签
IMAGE_NAME=dub_precedent:latest

# Docker 容器名称
CONTAINER_NAME=dub_precedent_app

# 容器内应用监听端口
APP_PORT=8080

# 宿主机映射端口，用于对外提供访问
HOST_PORT=8080

# 健康检查接口路径，用于探测服务是否正常
HEALTHCHECK_PATH=/api/health

# 生产构建命令，用于生成可部署产物
BUILD_COMMAND=npm run build:docker

# 生产启动命令，用于启动应用服务
START_COMMAND=npm run start

# 开发模式启动命令，用于本地调试或热更新
DEV_COMMAND=npm run dev

# APT 软件源镜像地址，用于加速系统依赖安装
APT_MIRROR=mirrors.tuna.tsinghua.edu.cn

# NPM 注册表镜像地址，用于加速 Node.js 依赖安装
NPM_REGISTRY=https://registry.npmmirror.com

# 必填环境变量列表，缺失时服务不应启动
REQUIRED_ENV_VARS=POSTGRES_PRISMA_URL POSTGRES_URL_NON_POOLING

# 是否启用默认路由覆盖，用于调整容器默认网络出口
ENABLE_DEFAULT_ROUTE_OVERRIDE=true

# 默认网关地址，用于容器网络路由配置
DEFAULT_GATEWAY=172.18.0.100

# PostgreSQL 所在的 Docker 网络名称，用于容器间通信
POSTGRES_DOCKER_NETWORK=1752417471_internal_network

# 是否启用 Chokidar 轮询监听，适用于容器内文件变更检测
CHOKIDAR_USEPOLLING=true

# 是否启用 Watchpack 轮询监听，适用于容器内热更新场景
WATCHPACK_POLLING=true

# ================================================================= 工程配置 =================================================================

# Prisma 使用的 PostgreSQL 连接字符串，支持连接池（Pgbouncer）和连接超时设置
POSTGRES_PRISMA_URL=postgresql://user:password@db-host:5432/app?pgbouncer=true&connect_timeout=15

# 非连接池模式下使用的 PostgreSQL 连接字符串
POSTGRES_URL_NON_POOLING=postgresql://user:password@db-host:5432/app
